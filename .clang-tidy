Checks: >
  bugprone-*,
  clang-analyzer-*,
  cppcoreguidelines-*,
  misc-*,
  modernize-*,
  performance-*,
  portability-*,
  readability-*,
  # Stuff you explicitly don't want:
  -readability-implicit-bool-conversion,
  -bugprone-easily-swappable-parameters,
  -modernize-use-trailing-return-type,
  -readability-identifier-naming,
  -readability-identifier-length,
  -readability-magic-numbers,
  -readability-implicit-bool-conversion,
  # cppcoreguidelines bits that are often noisy with Qt-ish / real-world code:
  #-cppcoreguidelines-avoid-magic-numbers,
  #-cppcoreguidelines-owning-memory,
  #-cppcoreguidelines-pro-type-reinterpret-cast

WarningsAsErrors: ''

# Only run checks on your project code under src/ and tests/,
# still excluding smoke.cpp as you had.
HeaderFilterRegex: '^(?!.*smoke\.cpp$)(src|tests)/.*\.(h|hpp|cpp)$'

FormatStyle: file
User: "GnotePad"

CheckOptions:
  # Ignore external libs in include-cleaner:
  - key: misc-include-cleaner.IgnoreHeaders
    value: '(.*/qt6/.*|.*/qsharedpointer_impl\.h|.*/spdlog/.*|.*/vcpkg/.*)'
